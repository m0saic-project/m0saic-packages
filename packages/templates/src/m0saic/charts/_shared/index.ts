/**
 * @m0saic/charts/_shared — drift-free numeric + geometry core shared by the
 * chart/graph templates (bar-graph, line-chart, …). Pure, deterministic, no
 * m0saic type deps. See scale.ts (Chart.js-faithful axis math) and line.ts
 * (line-series geometry + canonical SVG).
 */
export * from "./scale";
export * from "./line";
