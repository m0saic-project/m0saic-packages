{
  "name": "m0saic-packages",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/momo-types && npm run build -w packages/types && npm run build -w packages/platform && npm run build -w packages/text && npm run build -w packages/template-utils && npm run build -w packages/dictionary && npm run build -w packages/dsl-react && npm run build -w packages/templates && npm run build -w packages/knowledge",
    "test": "npm run test -w packages/momo-types && npm run test -w packages/platform && npm run test -w packages/text && npm run test -w packages/template-utils && npm run test -w packages/dictionary && npm run test -w packages/dsl-react && npm run test -w packages/templates && npm run test -w packages/knowledge"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "jest": "30.0.2",
    "ts-jest": "^29.4.6",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.0.3",
    "sharp": "^0.35.3",
    "@resvg/resvg-wasm": "^2.6.2"
  }
}
